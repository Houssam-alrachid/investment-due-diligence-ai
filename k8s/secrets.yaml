# ============================================================================
# SECRETS - Clés API et Données Sensibles
# ============================================================================
# IMPORTANT: Ce fichier contient des placeholders
# NE JAMAIS committer les vraies clés API dans Git!
# 
# Pour créer les secrets en production:
# kubectl create secret generic investment-ai-secrets \
#   --from-literal=OPENAI_API_KEY=sk-your-real-key \
#   --from-literal=SENDGRID_API_KEY=SG.your-real-key \
#   -n investment-ai
# ============================================================================

apiVersion: v1
kind: Secret
metadata:
  name: investment-ai-secrets
  namespace: investment-ai
  labels:
    app: investment-due-diligence
type: Opaque
stringData:
  # ATTENTION: Remplacer par vos vraies clés API
  # Ces valeurs sont des placeholders pour la démo
  OPENAI_API_KEY: "sk-placeholder-replace-with-real-key"
  SENDGRID_API_KEY: "SG.placeholder-replace-with-real-key"
